SSTACK SEGMENT STACK
    DW 32 DUP(?)
SSTACK ENDS

CODE SEGMENT
    ASSUME CS:CODE,SS:SSTACK
START:
    PUSH DS
    XOR AX,AX
    MOV SI,3000H
    MOV CX,16
AA1:
    MOV [SI],AL
    INC SI
    INC AL
    LOOP AA1
AA2:
    JMP AA2
CODE ENDS
    END START